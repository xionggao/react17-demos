const initialState = {

};

function receiveActions(state = initialState, {type, payload}) {
    switch (type) {
        default:
            break;
    }
    return state;
}

export default receiveActions;
